only through the count sore we can sort an array in O(n) complexity it will not work for large number array size like 10^9.
and space complexity will O(n)

=> when all odd elements are sorted and all even elements are sorted we can sperate out the even and ood and use merge 